# Dev image: installs deps, does NOT build.
# npm run dev (Next.js HMR) is the container command.
FROM node:22-alpine
WORKDIR /app

RUN apk add --no-cache wget

COPY package.json package-lock.json* .npmrc* ./
RUN npm ci

COPY . .
EXPOSE 8083
