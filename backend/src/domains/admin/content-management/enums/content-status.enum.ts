export enum ContentStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  SUSPENDED = 'suspended',
  FEATURED = 'featured',
}

export enum ContentType {
  COURSE = 'course',
  CHALLENGE = 'challenge',
  EVENT = 'event',
  POST = 'post',
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}
